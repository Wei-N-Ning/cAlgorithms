
void RunTinyTests();

////// data model //////
// node is modeled by a single ascii character
// nodes are held in a C array with fixed size
// connection is modeled by a char-int hash map
// so that:
// given a node (e.g. 'a'),
// the hash map stores the


void test_something() {
    ;
}

int main(int argc, char **argv) {
    RunTinyTests();
    return 0;
}
